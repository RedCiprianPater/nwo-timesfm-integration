--extra-index-url https://download.pytorch.org/whl/cpu

# Web Framework
flask==3.0.0
flask-limiter==3.5.0
gunicorn==21.2.0

# TimesFM and Dependencies (for future forecast endpoints)
timesfm
huggingface-hub>=0.19.0
transformers>=4.35.0

# Torch CPU-only — the --extra-index-url at the top is what keeps this small
torch>=2.0.0

# Scientific stack
numpy>=1.24.0
scipy>=1.10.0

# Data Processing
pandas>=2.0.0
pydantic>=2.0.0

# Utilities
python-dotenv==1.0.0
requests>=2.31.0

# Monitoring (optional)
prometheus-client==0.19.0

# EML symbolic regression (arXiv:2603.21852) — powers /residual-analysis
nwo-eml-regression[sympy] @ git+https://github.com/RedCiprianPater/nwo-eml-regression.git
