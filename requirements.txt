# Requirements for TimesFM NWO Integration

# Web Framework
flask==3.0.0
flask-limiter==3.5.0
gunicorn==21.2.0

# TimesFM and Dependencies
# Note: TimesFM requires specific versions
timesfm
huggingface-hub>=0.19.0
transformers>=4.35.0
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Data Processing
pandas>=2.0.0
pydantic>=2.0.0

# Utilities
python-dotenv==1.0.0
requests>=2.31.0

# Monitoring (optional)
prometheus-client==0.19.0

# Residual symbolic analysis — EML operator (arXiv:2603.21852).
# Used by src/routes/eml_residual.py and src/residual_analyzer.py.
# Swap for a pinned ==0.1.0 once the package is published to PyPI.
nwo-eml-regression[sympy] @ git+https://github.com/RedCiprianPater/nwo-eml-regression.git
